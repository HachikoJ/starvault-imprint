# 星仓印记 PRD

版本：0.1  
更新日期：2026-06-22  
文档定位：产品基线文档，用于约束后续迭代、验收和需求变更。

## 1. 产品定位

星仓印记，简称星仓，是一个 GitHub 开源机会研判中枢，用于从海量开源项目中发现值得学习、理解、持续跟踪和启发实践的项目。

产品不把项目简单理解为代码仓库列表，而是把每个仓库转化为可判断的机会单元：它解决什么问题、给谁使用、可能形成什么样的工具/服务/插件/模板/工作流，以及它的热度、质量、许可边界、风险和后续观察价值。

## 2. 背景

GitHub 每天产生大量新项目和更新项目，单靠 Trending、Star 数或人工搜索很难稳定发现真正值得关注的项目。用户面对的问题不是“项目太少”，而是“信息太多、分类混乱、价值判断不透明、许可边界不清楚、持续跟踪成本高”。

如果没有一个稳定的研判系统，用户容易出现四类问题：

- 被短期热度牵引，错过低调但有实践价值的项目。
- 被同类项目淹没，无法快速判断差异。
- 只看技术标签，看不出项目到底能解决什么真实问题。
- 忽略许可、维护、趋势和个人偏好，导致后续判断成本变高。

## 3. 用户目标

核心用户是个人开发者、产品型工程师、独立开发者和需要持续观察开源生态的人。

用户真正要完成的事情：

- 快速发现近期值得关注的 GitHub 项目。
- 一眼理解项目用途，而不是只看到语言、框架或抽象分类。
- 通过筛选、排序和榜单缩小关注范围。
- 通过详情、AI 分析和笔记形成自己的研判记录。
- 通过收藏、Star、Fork、笔记和反馈，让系统逐步理解个人偏好。
- 通过反信息茧房策略，避免推荐结果越来越窄。
- 在学习、借鉴、集成、发布或托管服务前看清许可边界和工程风险。

## 4. 产品原则

- 项目优先：简介、标签和详情必须围绕项目本身，而不是脱离项目做空泛分类。
- 三问清晰：列表和详情要优先回答“解决什么、给谁用、可形成什么实践形态”。
- 少即是清楚：高密度信息可以存在，但重点必须有层级，避免所有信息同时抢注意力。
- 逻辑可解释：评分、趋势、许可、风险和学习记忆不能完全黑盒，用户应能理解关键依据。
- 记忆可编辑：偏好不是系统单方面决定，用户必须能查看、增强、减弱、移除和清除。
- 保持探索：偏好会影响扫描和排序，但必须保留探索比例，避免只看熟悉方向。
- 许可有边界：公开仓库不等于可复制、修改、分发或托管使用，系统必须提示检查边界。
- 对外表达积极：产品传播口径使用学习、理解、跟踪、实践启发和机会研判，不使用功利化表述。

## 5. 范围

### 5.1 项目发现与扫描

- 支持手动扫描和每日定时扫描。
- 以 GitHub 搜索为主通道，结合多组查询画像发现候选项目。
- 支持 Tavily 和 Exa 作为外部信号补充。
- 支持 GitHub Token 以提高搜索和趋势缓存能力。
- 扫描过程展示阶段、进度、预计剩余时间和完成状态。
- 扫描完成后刷新项目池、榜单、结构看板和趋势缓存相关数据。

### 5.2 项目池

- 展示全量项目数、当前筛选命中数、当前页预览数。
- 支持分页、页码跳转、单页条数选择。
- 支持搜索、收藏过滤、语义标签过滤和排序。
- 语义标签至少包括作者、语言、解决什么、给谁用、可形成什么实践形态。
- 语义筛选必须明确区分项目池全量标签、当前筛选后可选标签和当前命中标签；标签按钮不直接塞入数量，当前可选数量放在分组标题旁，完整关系通过悬停提示表达。
- 点击项目标签后，应在当前项目池内筛选相关项目。
- 项目简介以中文为主，保留必要英文项目名和 GitHub 标识。
- 项目卡片展示 Star、Fork、趋势增量、收藏、访问、Star/Fork 操作状态。

### 5.3 项目详情

- 展示项目核心事实、简介、标签、趋势、许可边界、更多数据和用户研判记录。
- 支持复制地址、打开 GitHub、收藏、Star、Unstar、Fork。
- Fork 后展示已 Fork 状态，避免重复操作误导。
- 支持笔记新增、查看、更新和删除。
- 支持 AI 分析：用户可不填需求直接分析，也可填写特殊目标后综合分析。
- AI 分析结果应缓存；下次打开项目时直接展示上次结果，并支持再次分析。
- AI 分析结果应精炼，重点放在风险、边界和实践思路。

### 5.4 榜单

- 支持日榜、周榜、月榜、总榜。
- 每个榜单默认展示 20 个项目。
- 日榜支持日期归档，允许回溯历史榜单。
- 榜单项目可点击并跳转到项目池中的对应项目详情。
- 榜单应保持与项目池相近的信息结构，避免用户重新学习一套布局。

### 5.5 结构看板

- 只统计当前项目池中的项目。
- 展示方向结构、语言分布、许可分布和功能标签地图。
- 功能标签优先表达项目用途和能力，而不是低价值泛标签。
- 标签地图不得重复展示同一标签。

### 5.6 学习中枢

- 展示长期偏好、负向偏好、近期行为、反信息茧房策略、上下文压缩和项目进化评估。
- 用户行为包括选择项目、收藏、取消收藏、Star、Unstar、Fork、笔记、AI 分析和榜单反馈。
- 近期行为支持按 1 小时、1 天、1 周、1 月、3 个月和全部清除，并需要防误操作确认。
- 清除近期行为时，应联动回滚由这些行为产生的偏好影响；手动编辑的偏好不应被误删。
- 偏好会影响下一次扫描查询画像、标签排序和榜单排序。
- 反信息茧房策略需要可视化、可调节，并保持总比例逻辑清楚。
- 上下文压缩用于将大量短期行为沉淀为长期记忆，避免记忆无限膨胀。
- Harness 工程用于评估项目自身学习效果，而不是普通用户操作按钮集合。
- AI 调优应使用用户配置的大模型能力；当前默认适配 DeepSeek，界面使用通用 AI 表达。

### 5.7 设置与集成

- 设置页集中管理 GitHub、Tavily、Exa 和 AI Provider 配置。
- 每个 Key 支持独立显示、隐藏、清除和保存。
- Key 附近提供对应平台获取 Key 的入口。
- GitHub Token 获取入口指向 tokens classic。
- DeepSeek 是当前默认适配的大模型提供方；后续可扩展其他兼容协议。
- 支持拉取模型、测试连接和显示连接状态。
- 已配置 GitHub 并测试通过后，我的仓库页展示连接账号；仓库列表由用户主动刷新。

### 5.8 我的仓库

- 独立 Tab 展示用户 GitHub 仓库。
- 默认不自动拉取仓库，避免不必要请求。
- 用户点击刷新仓库后展示仓库列表。
- 支持对候选项目执行 Star、Unstar、Fork 等 GitHub 操作，并展示状态反馈。

### 5.9 导出

- 支持 JSON 和 CSV 导出。
- 顶部使用统一导出入口，由用户选择格式。
- 导出字段跟随当前界面语言。
- 导出内容应包含中文项目名、中文简介、GitHub 地址、作者、语言、核心语义标签、分数、趋势、许可、风险和用户记录。
- 导出不包含过时或不符合当前传播口径的字段。

### 5.10 国际化与体验

- 支持中文和英文切换。
- 中文界面不应混杂不必要英文；GitHub、Star、Fork、JSON、CSV 等通用技术词可保留。
- 英文界面所有文字必须完整显示，不得被按钮、容器或布局截断。
- 只有图标的按钮必须有悬停提示；已有文字的顶部 Tab 不需要重复提示。
- 高密度页面必须保证主次清晰，重要区域对齐，避免信息互相遮挡。
- UI 改动必须进入固定视觉回归矩阵，覆盖中英文、桌面/窄屏视口、项目池、榜单、学习中枢、结构看板、我的仓库和设置页。

## 6. 非范围

- 不提供法律意见，许可判断只作为工程风险提示。
- 不绕过项目许可限制，不鼓励复制受限项目。
- 不做多用户协作、团队权限、计费系统和公开 SaaS 部署能力。
- 不承诺覆盖 GitHub 全量事件流；大规模历史趋势需要 GH Archive、BigQuery 或独立数据管道。
- 不把 AI 输出当成事实来源；AI 只辅助研判，仓库元数据和许可事实优先。

## 7. 数据与状态

### 7.1 项目数据

项目数据来自 GitHub 搜索、仓库详情、外部搜索信号、趋势缓存、评分模型和用户行为。

核心字段：

- GitHub 基础信息：名称、作者、URL、描述、语言、Topics、Star、Fork、Issue、更新时间。
- 机会语义：解决什么、给谁用、可形成什么实践形态。
- 评分信息：推荐值、质量、社区、热度、行动准备度、风险等内部得分。
- 许可与风险：许可证、许可边界、项目风险、必要提示。
- 趋势缓存：前一日 Star/Fork 增量、统计日期、统计状态。
- 用户状态：收藏、笔记、AI 分析、Star/Fork 操作状态。

### 7.2 扫描数据

扫描记录应保存：

- 扫描时间、模式、状态、查询画像、命中数量。
- 项目入库数量、趋势更新数量、趋势计划数量。
- 错误列表和部分失败状态。

### 7.3 记忆数据

记忆分为：

- 短期行为：近期点击、收藏、笔记、AI 分析和榜单反馈。
- 长期偏好：从行为中沉淀的方向、用途、语言、许可和风险偏好。
- 负向偏好：用户不想频繁看到的方向。
- 反信息茧房策略：探索比例、多样性底线、新颖性比例。
- 压缩上下文：由短期行为归纳出的长期事实。

## 8. 评分与研判

项目推荐不能只看 Star。评分至少考虑：

- 当前关注度和近期趋势。
- 项目质量和维护状态。
- 社区活跃度。
- 项目用途是否清楚。
- 是否有明确用户、场景和实践形态。
- 许可边界是否清楚。
- 是否过度炒作或同质化严重。
- 是否符合用户长期偏好，同时保留探索空间。

具体权重属于内部实现，可以迭代，但 UI 文案必须避免让用户误以为单个分数就是绝对价值。

## 9. 关键用户流程

### 9.1 首次使用

1. 用户打开平台。
2. 查看顶部状态，了解 GitHub、Tavily、Exa、AI 是否配置。
3. 进入设置页填写 Key，并分别保存、测试。
4. 返回项目池，点击扫描。
5. 等待扫描进度完成，查看项目池和榜单。

### 9.2 发现项目

1. 用户查看项目池。
2. 通过搜索、标签、收藏和排序缩小范围。
3. 点击项目后查看详情。
4. 通过简介、标签、趋势、许可边界和 AI 分析判断是否值得持续关注。

### 9.3 沉淀偏好

1. 用户收藏、Star、Fork、写笔记或进行榜单反馈。
2. 学习中枢记录近期行为。
3. 系统更新长期偏好和负向偏好。
4. 下次扫描时，查询画像和排序结合偏好，但保留探索比例。

### 9.4 回溯与导出

1. 用户查看日榜归档或周/月/总榜。
2. 点击榜单项目跳转详情。
3. 对当前筛选结果导出 JSON 或 CSV。

## 10. 验收标准

正式验收以 [ACCEPTANCE_CHECKLIST.md](ACCEPTANCE_CHECKLIST.md) 为准。PRD 中每个范围项都应能在验收清单中找到对应检查项。

## 11. 风险

- GitHub API 限流会影响扫描速度、趋势缓存和仓库操作。
- 外部搜索服务可能返回噪声，需要评分和语义提取过滤。
- AI 分析可能出现幻觉，必须以仓库事实、许可事实和用户输入为边界。
- 记忆系统如果只强化用户偏好，会造成信息茧房。
- 标签自进化如果过于开放，会生成噪声标签；如果过于保守，会漏掉新机会类型。
- UI 信息密度高，布局稍有失衡就会造成阅读压力。
- 如果视觉回归测试没有固定矩阵，遮挡、错位、英文溢出和标签超框会反复回归。
- 本地 JSON 存储适合个人使用，生产化部署需要数据库和任务队列。

## 12. 当前 To-do

状态说明：

- `[x]`：源码或命令可证明已完成。
- `[~]`：能力已实现，但仍需要浏览器视觉、真实 GitHub Token、真实 AI Key 或长扫描验收。
- `[ ]`：尚未完成，或当前治理/生产化缺口仍然存在。
- `[!]`：当前状态不满足最低工程治理要求，需要优先处理。

| ID | 状态 | 事项 | 优先级 | 当前结论 | 验收入口 |
|---|---|---|---:|---|---|
| T-001 | [x] | 建立正式 PRD 和验收清单 | P0 | 已完成。PRD、验收清单、视觉回归矩阵和产品文档索引已存在。 | `PRD.md`、`ACCEPTANCE_CHECKLIST.md`、`README.md` |
| T-002 | [x] | 扫描主链路接入 GitHub Search、GitHub Trending、Tavily、Exa、评分、趋势缓存和入库 | P0 | 已完成主链路。GitHub Trending 已作为辅助发现和热度信号接入；趋势缓存随扫描更新。 | `src/server.js`、`src/lib/github.js` |
| T-003 | [x] | 项目池支持全量数、命中数、分页、页码跳转、标签筛选、排序和导出 | P0 | 已完成主要能力。项目总数与当前命中数在数据层已区分，分页和趋势排序已接入。 | 项目池验收项 P-001 至 P-013 |
| T-004 | [x] | 榜单支持日榜、周榜、月榜、总榜、每日归档和跳转详情 | P0 | 已完成。默认 20 个项目，日榜归档保存，榜单项目可跳转项目池详情。 | 榜单验收项 L-001 至 L-006 |
| T-005 | [x] | 学习中枢支持行为记忆、偏好编辑、清除回滚、上下文压缩、Harness 和 AI 调优入口 | P0 | 已完成核心能力。清除记录会清理 AI 分析、研判记录、收藏和本地 GitHub 操作记录，并回滚自动偏好到手动基线。 | 学习中枢验收项 M-003 至 M-006 |
| T-006 | [x] | 设置页集中管理 GitHub、Tavily、Exa 和 AI Provider Key | P0 | 已完成。Key 默认不明文返回，支持独立显示、清除、保存、模型拉取和连接测试。 | 设置验收项 S-001 至 S-006 |
| T-007 | [x] | 我的仓库独立 Tab，仓库列表由用户主动刷新 | P1 | 已完成代码入口。真实仓库读取仍需要有效 GitHub Token 做人工验收。 | 我的仓库验收项 G-001 至 G-004 |
| T-008 | [x] | 结构看板只统计当前项目池，并支持分布项反向筛项目池 | P1 | 已完成主要逻辑。方向结构、语言、许可、功能标签地图来自项目池数据，分布项可点击筛选。 | 结构看板验收项 B-001 至 B-005 |
| T-009 | [x] | 清理过度功利化旧传播口径 | P1 | 已完成主清理。当前扫描未发现旧口径外显残留；仍需继续扫尾少量“爆款/高潜”等早期定位词。 | 文案扫描、A-005 |
| T-010 | [~] | 动态标签升级为长期稳定的自进化 taxonomy | P0 | 已实现稳定核心标签 + 当前项目池动态标签 + emerging 候选 + 记忆加权排序，但仍需用包含新类型的测试项目池验收生成质量。 | Taxonomy 验收项 T-001 至 T-006 |
| T-011 | [~] | 趋势缓存缺失态、部分统计态和 0 增量态验收 | P0 | 已实现在线趋势缓存、缺失/失败/部分/0 增量状态和趋势排序，但需要真实扫描数据确认显示与统计准确性。 | 趋势验收项 SC-007、P-018、P-019 |
| T-012 | [~] | 扫描 ETA 在多阶段网络波动下的可信表达 | P1 | 已实现平滑 ETA、置信度和阶段提示，但 GitHub API 与外部搜索波动较大，需要长扫描观察。 | 扫描验收项 SC-003 至 SC-005 |
| T-013 | [~] | 固定视觉回归矩阵覆盖中英文、主要页面和多视口 | P1 | 脚本与矩阵已建立，但本轮未发现最新 `output/visual-regression/report.json`，需要跑通并留存报告。 | `npm run visual:check`、V-001 至 V-005 |
| T-014 | [ ] | 自动化业务测试覆盖筛选、分页、导出、记忆清除和榜单跳转 | P0 | 未完成。当前主要依赖源码检查、语法检查和视觉回归脚本，缺少数据层/接口层自动化断言。 | 新增测试脚本或 Node test |
| T-015 | [ ] | GitHub API 限流、二级限流和失败重试策略生产化 | P1 | 未完成。当前有并发限制和错误状态，但缺少系统性退避、限流预算、失败重试和扫描节流策略。 | 扫描长跑验收、GitHub 错误日志 |
| T-016 | [ ] | README、TAXONOMY、RISK_MODEL、LICENSE_POLICY 与当前 PRD 持续同步 | P1 | 部分完成。主文档已较新，但仍有 `high-potential`、`爆款潜力`、`practical adoption` 等口径需要二次校准。 | 文案扫描、人工阅读 |
| T-017 | [!] | 建立 Git 版本基线和提交安全网 | P0 | 当前不满足。`git status --short` 显示项目文件仍处于未跟踪状态，无法可靠追踪变更、回滚和审查。 | `git status --short` |
| T-018 | [ ] | 生产化存储从本地 JSON 迁移到数据库和任务队列 | P2 | 未完成。当前本地 JSON 适合个人使用，生产化部署需要 Postgres/SQLite、任务队列和扫描任务状态持久化。 | 生产化架构验收 |

## 13. 当前查漏补缺清单

### 13.1 P0：必须优先处理

- 建立 Git 基线：把当前项目纳入版本控制，避免后续 UI 和逻辑修改无法追踪。
- 补齐接口/数据层自动化测试：至少覆盖项目池筛选分页、动态标签、导出字段、学习清除、榜单跳转和设置密钥默认隐藏。
- 跑通并留存视觉回归报告：确认中英文、多视口、主要页面没有遮挡、截断、横向滚动和 Tooltip 出界。

### 13.2 P1：影响质量和可信度

- 对 GitHub API 限流做更完整治理：扫描预算、失败重试、指数退避、部分失败提示和下一次重试策略。
- 用真实 GitHub Token 验收 Star、Unstar、Fork、我的仓库刷新和趋势缓存。
- 用真实 AI Key 验收项目 AI 分析、模型拉取、连接测试和学习中枢 AI 调优。
- 用真实扫描数据验收趋势的 `0`、缺失、失败、部分统计状态，避免用户误解。
- 二次清理传播口径：保留学习、理解、跟踪、实践启发和机会研判，减少早期“爆款/高潜/功利化”措辞。
- 同步 README、TAXONOMY、RISK_MODEL、LICENSE_POLICY，避免文档落后于当前产品逻辑。

### 13.3 P2：生产化增强

- 将本地 JSON 存储迁移到数据库，并把扫描任务放入队列。
- 引入更稳定的历史趋势数据源，例如 GH Archive、BigQuery 或独立事件采集。
- 为 taxonomy 自进化增加可审计面板：展示新增标签来源、命中样本、合并/禁用/重命名记录。

## 14. 变更规则

- 新功能必须先更新 PRD 或验收清单，再进入实现。
- 如果实现与 PRD 冲突，以当前用户最新确认的需求为准，并同步修订文档。
- 删除功能或改变核心口径前，需要说明影响范围和回滚方式。
- 每次重要 UI 或逻辑迭代后，应检查 README、TAXONOMY、RISK_MODEL、LICENSE_POLICY、PRD 和验收清单是否需要同步。
