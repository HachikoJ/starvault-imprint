# 星仓印记 PRD

版本：0.3
更新日期：2026-07-18
文档定位：产品基线文档，用于约束后续迭代、验收和需求变更。

## 1. 产品定位

星仓印记，简称星仓，是一个 GitHub 开源机会研判中枢，用于从海量开源项目中发现值得学习、理解、持续跟踪和启发实践的项目。

产品不把项目简单理解为代码仓库列表，而是把每个仓库转化为可判断的机会单元：它解决什么问题、给谁使用、可能形成什么样的工具/服务/插件/模板/工作流，以及它的热度、质量、许可边界、风险和后续观察价值。

## 2. 背景

GitHub 每天产生大量新项目和更新项目，单靠 Trending、Star 数或人工搜索很难稳定发现真正值得关注的项目。用户面对的问题不是“项目太少”，而是“信息太多、分类混乱、价值判断不透明、许可边界不清楚、持续跟踪成本高”。

如果没有一个稳定的研判系统，用户容易出现四类问题：

- 被短期热度牵引，错过低调但有实践价值的项目。
- 被同类项目淹没，无法快速判断差异。
- 只看技术标签，看不出项目到底能解决什么真实问题。
- 忽略许可、维护、趋势和个人偏好，导致后续判断成本变高。

## 3. 用户目标

核心用户是个人开发者、产品型工程师、独立开发者和需要持续观察开源生态的人。

用户真正要完成的事情：

- 快速发现近期值得关注的 GitHub 项目。
- 一眼理解项目用途，而不是只看到语言、框架或抽象分类。
- 通过筛选、排序和榜单缩小关注范围。
- 通过详情、AI 分析和笔记形成自己的研判记录。
- 通过收藏、Star、Fork、笔记和反馈，让系统逐步理解个人偏好。
- 通过反信息茧房策略，避免推荐结果越来越窄。
- 在学习、借鉴、集成、发布或托管服务前看清许可边界和工程风险。

## 4. 产品原则

- 项目优先：简介、标签和详情必须围绕项目本身，而不是脱离项目做空泛分类。
- 三问清晰：列表和详情要优先回答“解决什么、给谁用、可形成什么实践形态”。
- 少即是清楚：高密度信息可以存在，但重点必须有层级，避免所有信息同时抢注意力。
- 逻辑可解释：评分、趋势、许可、风险和学习记忆不能完全黑盒，用户应能理解关键依据。
- 记忆可编辑：偏好不是系统单方面决定，用户必须能查看、增强、减弱、移除和清除。
- 保持探索：偏好会影响扫描和排序，但必须保留探索比例，避免只看熟悉方向。
- 许可有边界：公开仓库不等于可复制、修改、分发或托管使用，系统必须提示检查边界。
- 对外表达积极：产品传播口径使用学习、理解、跟踪、实践启发和机会研判，不使用功利化表述。

## 5. 范围

### 5.1 项目发现与扫描

- 支持手动扫描和每日定时扫描。
- 以 GitHub 搜索为主通道，结合多组查询画像发现候选项目。
- 支持 Tavily 和 Exa 作为外部信号补充。
- 支持 GitHub Token 以提高搜索和趋势缓存能力。
- 扫描过程展示阶段、进度、预计剩余时间和完成状态。
- 扫描完成后刷新项目池、榜单、结构看板和趋势缓存相关数据。

### 5.2 项目池

- 展示全量项目数、当前筛选命中数、当前页预览数。
- 支持分页、页码跳转、单页条数选择。
- 支持搜索、收藏过滤、语义标签过滤和排序。
- 语义标签至少包括作者、语言、解决什么、给谁用、可形成什么实践形态。
- 语义筛选必须明确区分项目池全量标签、当前筛选后可选标签和当前命中标签；标签按钮不直接塞入数量，当前可选数量放在分组标题旁，完整关系通过悬停提示表达。
- 点击项目标签后，应在当前项目池内筛选相关项目。
- 项目简介以中文为主，保留必要英文项目名和 GitHub 标识。
- 项目卡片展示 Star、Fork、趋势增量、收藏、访问、Star/Fork 操作状态。

### 5.3 项目详情

- 展示项目核心事实、简介、标签、趋势、许可边界、更多数据和用户研判记录。
- 支持复制地址、打开 GitHub、收藏、Star、Unstar、Fork。
- Fork 后展示已 Fork 状态，避免重复操作误导。
- 支持笔记新增、查看、更新和删除。
- 支持 AI 分析：用户可不填需求直接分析，也可填写特殊目标后综合分析。
- AI 分析结果应缓存；下次打开项目时直接展示上次结果，并支持再次分析。
- AI 分析结果应精炼，重点放在风险、边界和实践思路。

### 5.4 榜单

- 支持日榜、周榜、月榜、总榜。
- 每个榜单默认展示 20 个项目。
- 日榜支持日期归档，允许回溯历史榜单。
- 榜单项目可点击并跳转到项目池中的对应项目详情。
- 榜单应保持与项目池相近的信息结构，避免用户重新学习一套布局。

### 5.5 结构看板

- 只统计当前项目池中的项目。
- 展示方向结构、语言分布、许可分布和功能标签地图。
- 功能标签优先表达项目用途和能力，而不是低价值泛标签。
- 标签地图不得重复展示同一标签。

### 5.6 学习中枢

- 展示长期偏好、负向偏好、近期行为、反信息茧房策略、上下文压缩和项目进化评估。
- 用户行为包括收藏、取消收藏、隐藏、恢复、Star、Unstar、Fork、笔记、AI 分析和榜单反馈；选择项目只保留近期浏览留痕，不参与偏好学习。
- 近期行为支持按 1 小时、1 天、1 周、1 月、3 个月和全部清除，并需要防误操作确认。
- 清除近期行为时，应联动回滚由这些行为产生的偏好影响；手动编辑的偏好不应被误删。
- 偏好会影响下一次扫描查询画像、标签排序和榜单排序。
- 反信息茧房策略需要可视化、可调节，并保持总比例逻辑清楚。
- 上下文压缩用于将大量短期行为沉淀为长期记忆，避免记忆无限膨胀。
- Harness 工程用于评估项目自身学习效果，而不是普通用户操作按钮集合。
- AI 调优应使用用户配置的大模型能力；当前默认适配 DeepSeek，界面使用通用 AI 表达。

### 5.7 设置与集成

- 设置页集中管理 GitHub、Tavily、Exa 和 AI Provider 配置。
- 每个 Key 支持独立显示、隐藏、清除和保存。
- Key 附近提供对应平台获取 Key 的入口。
- GitHub Token 获取入口指向 tokens classic。
- DeepSeek 是当前默认适配的大模型提供方；后续可扩展其他兼容协议。
- 支持拉取模型、测试连接和显示连接状态。
- 已配置 GitHub 并测试通过后，我的仓库页展示连接账号；仓库列表由用户主动刷新。

### 5.8 我的仓库

- 独立 Tab 展示用户 GitHub 仓库。
- 默认不自动拉取仓库，避免不必要请求。
- 用户点击刷新仓库后展示仓库列表。
- 支持对候选项目执行 Star、Unstar、Fork 等 GitHub 操作，并展示状态反馈。

### 5.9 导出

- 支持 JSON 和 CSV 导出。
- 顶部使用统一导出入口，由用户选择格式。
- 导出字段跟随当前界面语言。
- 导出内容应包含中文项目名、中文简介、GitHub 地址、作者、语言、核心语义标签、分数、趋势、许可、风险和用户记录。
- 导出不包含过时或不符合当前传播口径的字段。

### 5.10 国际化与体验

- 支持中文和英文切换。
- 中文界面不应混杂不必要英文；GitHub、Star、Fork、JSON、CSV 等通用技术词可保留。
- 英文界面所有文字必须完整显示，不得被按钮、容器或布局截断。
- 只有图标的按钮必须有悬停提示；已有文字的顶部 Tab 不需要重复提示。
- 高密度页面必须保证主次清晰，重要区域对齐，避免信息互相遮挡。
- UI 改动必须进入固定视觉回归矩阵，覆盖中英文、桌面/窄屏视口、项目池、榜单、学习中枢、结构看板、我的仓库和设置页。

## 6. 非范围

- 不提供法律意见，许可判断只作为工程风险提示。
- 不绕过项目许可限制，不鼓励复制受限项目。
- 不做多用户协作、团队权限、计费系统和公开 SaaS 部署能力。
- 不承诺覆盖 GitHub 全量事件流；大规模历史趋势需要 GH Archive、BigQuery 或独立数据管道。
- 不把 AI 输出当成事实来源；AI 只辅助研判，仓库元数据和许可事实优先。

## 7. 数据与状态

### 7.1 项目数据

项目数据来自 GitHub 搜索、仓库详情、外部搜索信号、趋势缓存、评分模型和用户行为。

核心字段：

- GitHub 基础信息：名称、作者、URL、描述、语言、Topics、Star、Fork、Issue、更新时间。
- 机会语义：解决什么、给谁用、可形成什么实践形态。
- 评分信息：推荐值、质量、社区、热度、行动准备度、风险等内部得分。
- 许可与风险：许可证、许可边界、项目风险、必要提示。
- 趋势缓存：前一日 Star/Fork 增量、统计日期、统计状态。
- 用户状态：收藏、笔记、AI 分析、Star/Fork 操作状态。

### 7.2 扫描数据

扫描记录应保存：

- 扫描时间、模式、状态、查询画像、命中数量。
- 项目入库数量、趋势更新数量、趋势计划数量。
- 错误列表和部分失败状态。

### 7.3 记忆数据

记忆分为：

- 短期行为：近期浏览留痕、收藏、隐藏、笔记、AI 分析和榜单反馈；浏览留痕权重为零，不改变偏好或检索画像。
- 长期偏好：从行为中沉淀的方向、用途、语言、许可和风险偏好。
- 负向偏好：用户不想频繁看到的方向。
- 反信息茧房策略：探索比例、多样性底线、新颖性比例。
- 压缩上下文：由短期行为归纳出的长期事实。

### 7.4 本地存储与任务状态

- Node 本地服务以 SQLite WAL 为主存储，项目、观察方案、扫描记录和方案归属关系按记录持久化；旧 `store.json` 只作为一次性迁移来源和人工备份，不再是运行时主库。
- Web 静态模式使用 IndexedDB v5，项目、观察方案、扫描记录、榜单归档和长扫描临时结果分别存入对象仓库；Node 到浏览器的同步使用核心快照、250 条项目分页和 10 条榜单分页。
- API Key 和 Token 不进入 portable export、核心快照、项目分页或榜单分页；浏览器本地密钥单独保存在 IndexedDB，Node 密钥保存在本地 SQLite 状态中。
- 扫描、AI 分析和方案生成使用持久化任务记录。刷新页面不会取消任务；进程重启后，运行中任务会回到队列并最多重试 3 次。浏览器扫描会把每个已完成 GitHub profile 的结果暂存到 IndexedDB，刷新后跳过已完成 profile。
- 任务恢复不能从已经中断的单个 HTTP 请求内部续传；未完成的当前请求会重试，已完成的扫描 profile、Node 扫描阶段和任务方案绑定信息必须保留，避免重复请求与跨方案污染。

## 8. 评分与研判

项目推荐不能只看 Star。评分至少考虑：

- 当前关注度和近期趋势。
- 项目质量和维护状态。
- 社区活跃度。
- 项目用途是否清楚。
- 是否有明确用户、场景和实践形态。
- 许可边界是否清楚。
- 是否过度炒作或同质化严重。
- 是否符合用户长期偏好，同时保留探索空间。

具体权重属于内部实现，可以迭代，但 UI 文案必须避免让用户误以为单个分数就是绝对价值。

## 9. 关键用户流程

### 9.1 首次使用

1. 用户打开平台。
2. 查看顶部状态，了解 GitHub、Tavily、Exa、AI 是否配置。
3. 进入设置页填写 Key，并分别保存、测试。
4. 返回项目池，点击扫描。
5. 等待扫描进度完成，查看项目池和榜单。

### 9.2 发现项目

1. 用户查看项目池。
2. 通过搜索、标签、收藏和排序缩小范围。
3. 点击项目后查看详情。
4. 通过简介、标签、趋势、许可边界和 AI 分析判断是否值得持续关注。

### 9.3 沉淀偏好

1. 用户收藏、Star、Fork、写笔记或进行榜单反馈。
2. 学习中枢记录近期行为。
3. 系统更新长期偏好和负向偏好。
4. 下次扫描时，查询画像和排序结合偏好，但保留探索比例。

### 9.4 回溯与导出

1. 用户查看日榜归档或周/月/总榜。
2. 点击榜单项目跳转详情。
3. 对当前筛选结果导出 JSON 或 CSV。

## 10. 验收标准

正式验收以 [ACCEPTANCE_CHECKLIST.md](ACCEPTANCE_CHECKLIST.md) 为准。PRD 中每个范围项都应能在验收清单中找到对应检查项。

## 11. 风险与明确边界

- GitHub 主限额和 secondary rate limit 仍会影响扫描时长。实现已在扫描前读取预算、限制并发、串行搜索、识别阻断错误并按官方建议退避，但无法消除上游平台限流。
- “任意领域泛化”不能被数学证明。当前证据是无固定领域词包的统一提示词、生成质量门、10 类陌生领域契约矩阵，以及可通过 `--cases-file` 扩展的真实 AI 审计；发布前仍需抽样用户真实领域。
- 学习中枢会影响默认方案的补充查询、所有方案的 profile 顺序、候选预算、入池排序和榜单排序。自定义方案不会注入跨领域记忆词，以避免把 CAD 偏好带进 CRM；因此学习影响受方案语义边界约束。
- SQLite 仍在 Node 进程启动时装载项目到内存。6,263 个项目合成基准本轮堆内存约 133.7 MB，当前个人规模仍在预算内；若增长到数万项目，需要把筛选、排序和摘要继续下推到 SQL。
- IndexedDB 已拆成记录级对象仓库，但浏览器仍会在需要完整方案项目池时读取相关记录。分页同步解决网络与写入峰值，不等同于无限规模数据库；扫描临时结果只在任务完成或失败后清理。
- 持久化任务可在刷新或进程重启后恢复：扫描跳过已完成 profile，AI/方案生成在中断的外部请求处重试；外部 API 写操作必须保持幂等和方案绑定。
- Node 服务具备应用层认证、Origin、SSRF、CSP、限流和输入边界，但不承担公网 DDoS、账号体系、租户隔离和密钥托管。直接把 Node 端口暴露到公网仍属于不支持部署方式。
- 本地密钥当前是本机明文静态数据：Node 在 SQLite、浏览器在 IndexedDB。设备被入侵或同源脚本被攻破时无法保证密钥安全；桌面发布应接 macOS Keychain，公网服务应接 Secret Manager。
- AI 和外部搜索结果可能有噪声或幻觉，仓库事实、许可证原文和用户输入始终优先。

## 12. 2026-07-10 发布复盘

状态说明：`[x]` 已有源码与自动化证据；`[~]` 实现已完成但仍依赖真实外部服务抽样；`[ ]` 尚未达到支持范围。

| ID | 状态 | 事项 | 结论与证据 |
|---|---|---|---|
| R-001 | [x] | Git 基线 | 已建立初始提交 `bedb953`，后续改造可审查、回滚和比较。 |
| R-002 | [x] | 默认方案隔离 | 默认方案只读取默认归属或兼容旧默认数据；自定义方案扫描替换自己的匹配，删除方案会清理无其他归属的孤立项目。对应查询、删除和回归测试已覆盖。 |
| R-003 | [x] | Node 存储迁移 | 运行时主库为 SQLite WAL；项目、方案、扫描和归属关系按表增量写入，旧 JSON 保持只读迁移备份。 |
| R-004 | [x] | Web IndexedDB 迁移 | IndexedDB v5 使用 `projects/plans/scans/leaderboards/taskArtifacts/meta/kv` 对象仓库；核心、项目、榜单和扫描临时结果分别同步，不再写入完整项目池 JSON。 |
| R-005 | [x] | 数据规模适配 | 6,263 项目合成库复测：冷加载 54.8 ms，当前方案列表 1,218.4 ms，摘要 849.7 ms，核心快照 106,448 B，单条增量写入 69 ms。预算由 `npm run perf:storage:fixture` 复验；本地真实数据应另跑 `npm run perf:storage`。 |
| R-006 | [~] | 任意领域生成 | 已删除历史领域词包和运行时特殊分支，统一经过元认知扩展、锚点校验、查询质量门和最多 30 条可见即执行规则；10 类陌生领域自动化契约通过。真实模型输出仍需 `npm run audit:plans:live` 持续抽样。 |
| R-007 | [x] | 学习驱动发现 | 被动查看只保留零权重浏览留痕，收藏 `2.6`、Star `3.2`、Fork `4.2`；自定义方案内的正负行为会调整 profile 顺序与 `0.7-2.2` 候选预算，负向信号不会删除探索槽位。 |
| R-008 | [x] | 可恢复长任务 | 扫描、AI 分析、方案生成持久化；扫描按 profile 保存检查点并在刷新后跳过已完成 profile，AI/方案生成从中断请求重试；进程重启后重新排队，最多重试 3 次，任务结果写入创建时方案。 |
| R-009 | [~] | 公网安全边界 | 非 loopback 监听强制 `AUTH_TOKEN`，并有严格 Origin、可信代理开关、SSRF、URL 白名单、CSP、请求限制与分级限流。公网仍必须有 TLS、反向代理/CDN/WAF、审计和外部密钥管理。 |
| R-010 | [x] | 质量门禁 | `npm run ci` 覆盖语法和全部 Node 测试；GitHub Actions 另跑 Chromium 视觉矩阵、6,263 项目合成存储预算并上传视觉报告；`npm run release:check` 组合源码、视觉和无用户数据的性能门禁。 |

## 13. 剩余发布前验证

- 用有效和失效 GitHub Token 各跑一次完整扫描，确认主限额、secondary rate limit、Star/Unstar/Fork 和“我的仓库”路径。
- 用真实 AI Key 对至少 10 个不在内置审计矩阵中的领域运行 `npm run audit:plans:live -- --cases-file <file>`，记录 PASS/WARN/FAIL，而不是宣称一次性证明任意领域。
- 在目标部署环境运行 `npm run perf:storage`；若项目量明显超过当前 6K 基线，重新评估 Node 全量内存模型。
- 公网 Web 只发布静态本地模式，或把 Node 服务放在认证网关、TLS、CDN/WAF 和 Secret Manager 后；不支持裸端口公开。
- macOS 打包前把桌面密钥迁移到 Keychain，并补签名、公证、自动更新和沙盒权限验收。
- 每次发布运行 `npm run release:check`，并人工抽查生成的项目池、榜单、英文布局和真实外部服务结果。

## 14. 变更规则

- 新功能必须先更新 PRD 或验收清单，再进入实现。
- 如果实现与 PRD 冲突，以当前用户最新确认的需求为准，并同步修订文档。
- 删除功能或改变核心口径前，需要说明影响范围和回滚方式。
- 每次重要 UI 或逻辑迭代后，应检查 README、TAXONOMY、RISK_MODEL、LICENSE_POLICY、PRD 和验收清单是否需要同步。
