# Visual Regression Matrix

更新日期：2026-07-10
目标：把历史上反复出现的遮挡、错位、英文溢出、标签超框和 Tooltip 位置问题变成固定验收项。

## 1. 执行命令

```bash
npm run visual:check
```

首次运行前需要安装 Playwright：

```bash
npm install --save-dev playwright
npx playwright install chromium
```

脚本会在系统临时目录生成无密钥样本库，启动独立本地服务 `http://127.0.0.1:4179`，结束后删除样本库。它不会读取 `data/`、恢复用户任务、触发真实扫描或请求 GitHub/AI Provider。

输出位置：

- `output/visual-regression/screenshots/`
- `output/visual-regression/report.json`

## 2. 固定矩阵

| 维度 | 覆盖项 |
|---|---|
| 语言 | 中文、英文 |
| 视口 | 1440x900、1280x800、900x900、390x844 |
| 页面 | 项目池、榜单、学习中枢、结构看板、我的仓库、设置 |
| 重点状态 | 项目池默认态、项目详情打开态、榜单列表态、设置密钥区、学习中枢高密度信息区 |

## 3. 自动失败条件

| 类型 | 说明 |
|---|---|
| 控制台错误 | 页面出现 `console.error` 或未捕获异常 |
| 横向溢出 | 页面或关键容器宽度超过当前视口 |
| 文本截断 | 高风险按钮、标签、统计卡、分页、设置项文字被容器裁切 |
| 同级遮挡 | 顶部操作区、分页区、仓库操作区、密钥按钮区等同级元素互相覆盖 |
| Tooltip 失控 | Tooltip 超出视口，或覆盖被悬停的触发按钮 |

有明确 `text-overflow: ellipsis` 的单行摘要和 CSS line-clamp 内容属于有意摘要，不计为失败；其容器位置、相邻遮挡和横向溢出仍继续检查。

## 4. 高风险组件

这些区域每次 UI 改动后必须重点看截图：

- 顶部品牌、Tab、配置状态、语言、导出、扫描。
- 项目池筛选侧栏，尤其是动态语义标签、许可标签、排序标签。
- 项目列表卡片，尤其是项目名、简介、作者/语言/语义标签、Star/Fork/趋势行。
- 项目池分页，尤其是页码、跳转、单页条数选择。
- 项目详情操作按钮、AI 分析、更多数据、研判记录。
- 榜单头部、日榜存档、榜单说明和榜单项目卡片。
- 学习中枢的长期偏好、负向偏好、近期行为、反信息茧房、上下文压缩。
- 设置页的 GitHub/Tavily/Exa/AI Key 管理、获取 Key 链接和检索逻辑说明。
- 站点页脚的官网和 GitHub 仓库入口，桌面端常驻、移动端在页面底部。

## 5. 人工复核标准

自动脚本只能抓硬性布局错误，不能判断审美层级。每次较大 UI 改动还需要人工复核：

- 一屏内是否有清楚主次，而不是所有信息都像重点。
- 卡片、表单、列表、说明文字是否有稳定间距和对齐。
- 中文和英文是否都自然，不靠缩小字体硬塞。
- 标签是否真的帮助理解项目，而不是只制造噪声。
- 空状态、失败状态、加载状态是否能让用户知道下一步做什么。

## 6. 通过标准

一次 UI 迭代至少满足：

- `npm run check` 通过。
- `npm run visual:check` 通过，或明确记录失败项和决定暂缓的原因。
- GitHub Actions 的 visual job 通过并上传 `output/visual-regression/` artifact。
- 对 `output/visual-regression/screenshots/` 中本次矩阵截图做过人工抽查。
- 如果修改了布局、文案、标签或国际化，PRD 和验收清单中相关条目仍然成立。
