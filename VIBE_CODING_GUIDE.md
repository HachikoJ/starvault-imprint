# Vibe Coding AI 执行规范

这是一份给 AI coding agent 直接执行的协作规范。用于通过 vibe coding
进行产品构建、功能开发、代码修改、调试和审查。

## 核心立场

Vibe coding 的目标是快速推进产品，而不是放弃控制。速度来自 AI，控制来自
PRD、Todo、源码理解、风险边界和验证闭环。

源码是实现真相。PRD、功能 Todo、设计意图和验收标准是方向真相。

不要维护容易过期的长篇实现文档。必须维护短而稳定的产品意图、业务约束、
设计决策、风险边界和验收标准。

## 执行口诀

PRD 定方向，Todo 定节奏，源码定真相，测试定完成。
不清楚就问，高风险就停，改了就验证，跑偏就回 PRD。

## 每次行动前的状态报告

非平凡改动前，AI 必须先报告当前状态：

| 字段 | 必填内容 |
|---|---|
| 阶段 | PRD、Todo、读源码、计划、实现、验证、审查 |
| 目标 | 当前要达成的用户可见结果 |
| 假设 | 正在基于什么假设行动，为什么合理 |
| 风险 | 安全、数据、体验、回归、成本、可维护性风险 |
| 下一步 | 立刻要做什么，以及如何验证 |

如果目标、范围或风险不清楚，先停下来问，不要直接改代码。

## 需求入口闸门

新功能、复杂修改或多文件改动必须先过需求闸门：

1. 复述需求：用自己的话说明目标、用户、范围、非范围。
2. 提出问题：只问会影响实现方向或风险判断的问题。
3. 更新 PRD：把确认后的需求写入 PRD 或本任务说明。
4. 等待许可：用户明确同意后再进入实现。
5. 拆 Todo：把需求拆成可独立验证的小任务。

| 触发条件 | 必须动作 |
|---|---|
| 新功能或复杂体验 | 先写 PRD 或方案，不直接写代码 |
| 预计修改超过 3 个文件 | 先列文件级修改计划并等待确认 |
| 新增能力 | 先搜索是否已有类似实现，优先复用或扩展 |
| 需求变更 | 先更新 PRD 的范围、非范围和变更记录 |
| 外部 API、数据结构、新 UI 区域 | 先讨论方案，再实现 |

禁止在没有理解现有实现的情况下重复造轮子。

## 阶段规则

| 阶段 | 必做事项 | 验证方式 |
|---|---|---|
| PRD | 明确背景、用户目标、范围、非范围、UX、数据、风险、验收标准 | 每条需求都有可验证的验收规则 |
| Todo | 把 PRD 拆成小功能任务 | 每个 Todo 有责任文件、完成标准和状态 |
| 读源码 | 阅读相关源码和现有模式 | 计划能引用真实文件和本地惯例 |
| 计划 | 给出最小安全实现方案 | 用户能看到改哪些文件、有什么风险、怎么验 |
| 实现 | 只做必要的手术式修改 | diff 能对应到当前 Todo |
| 验证 | 按需跑测试、lint、build、截图或手动检查 | 不隐藏失败，不假装完成 |
| 审查 | 总结改动、证据、风险和后续事项 | 不做未验证的完成声明 |

## PRD 模板

产品或功能开发必须使用这个最小 PRD：

| 模块 | 必填内容 |
|---|---|
| 背景 | 为什么要做，不做会怎样 |
| 用户目标 | 用户真正想完成什么 |
| 范围 | 本次明确要做什么 |
| 非范围 | 本次明确不做什么，防止 AI 加戏 |
| 用户流程 | 主路径、空状态、错误状态、边界情况 |
| 数据与状态 | 数据来源、字段语义、权限、生命周期 |
| UI 规则 | 布局、组件、图标库、状态、无障碍要求 |
| 验收标准 | 能证明完成的具体检查 |
| 风险 | 安全、隐私、性能、成本、模型漂移、兼容性 |
| 变更记录 | AI 提出的需求变更必须先写回这里，再实现 |

没有写回 PRD 的需求变更，不得直接实现。

## 功能 Todo 模板

执行阶段使用功能 Todo：

| ID | 功能 | 背景 | 文件 / 模块 | 完成标准 | 验证 | 状态 |
|---|---|---|---|---|---|---|
| F-001 | 简短功能名 | 为什么存在 | 预计相关文件 | 用户可观察的完成规则 | 测试、build、截图或手动检查 | Todo |

Todo 规则：

- 每个 Todo 必须小到可以独立验证。
- 状态只使用 Todo、Doing、Blocked、Done。
- 不把无关功能塞进同一个 Todo。
- 如果 Todo 暴露需求变化，先更新 PRD，再继续实现。

## 编码规则

| 规则 | 指令 |
|---|---|
| 最小改动 | 只触碰当前 Todo 必须修改的文件 |
| 匹配现状 | 遵循仓库现有风格、命名、架构和工具 |
| 不做猜想 | 不添加未要求的灵活性、配置项或功能 |
| 不顺手重构 | 除非当前 Todo 必须，否则不重构相邻代码 |
| 先读源码 | 改代码前必须读相关源码 |
| 必须验证 | 改完运行最窄但可靠的验证 |
| 说明限制 | 不能验证时，说明原因和剩余风险 |
| 不做临时方案 | 不用「先 Mock」「后续优化」替代完整正确实现 |

如果完整实现成本过高，必须说明真实代价、风险和需要用户决策的点，不能自行降级。

## 函数注释规则

函数注释必须解释设计背景，而不是复述代码功能。

正确示例：

```ts
/**
 * 设计背景：结算页允许游客先加购再登录，所以这里合并购物车，
 * 而不是用服务端购物车直接覆盖离线购物车。
 * 业务约束：同 SKU 冲突时保留用户最近一次操作。
 * 风险提示：修改合并策略会影响支付前价格校验。
 */
function mergeCheckoutCart(...) {}
```

错误示例：

```ts
/**
 * 合并两个购物车并返回结果。
 */
function mergeCheckoutCart(...) {}
```

注释必须说明：

- 这个函数为什么存在。
- 背后的业务、产品或体验约束。
- 修改它可能影响的下游风险。
- 不要复述显而易见的控制流。

## 变量命名规则

跨函数、跨组件、跨文件或进入状态管理的变量，必须包含业务背景。

| 弱命名 | 更好的命名 |
|---|---|
| `items` | `pendingDeletionApprovalItems` |
| `source` | `checkoutCartMergeSource` |
| `enabled` | `isWebhookRetryEnabledForUserInput` |
| `status` | `scanComplianceReviewStatus` |

局部循环变量在上下文明确时可以保持简单。

## 删除审批规则

AI 不得擅自删除代码、注释、测试、字段、迁移、配置、日志或安全检查。

删除前必须提交审批表：

| 审批项 | 必填内容 |
|---|---|
| 删除对象 | 精确到文件、函数、字段、测试或注释 |
| 删除原因 | 为什么必须删除 |
| 影响范围 | 调用方、数据、UI、测试、兼容性、回滚风险 |
| 替代方案 | 保留、废弃、重命名、隔离或替换是否可行 |
| 验证方式 | 如何证明删除是安全的 |

用户未明确批准前，不得删除。

## UI 与图标规则

AI 很容易滥用 emoji。产品 UI 必须使用专业图标库和真实视觉资产。

| 场景 | 使用 | 避免 |
|---|---|---|
| 按钮和工具 | Tabler Icons、Lucide、Heroicons 或项目既有图标系统 | emoji |
| 状态标识 | Badge、颜色 token、图标、tooltip | 只靠 emoji 表达状态 |
| 导航 | 项目图标系统，必要时配文字 | 装饰性 emoji |
| 游戏或品牌资产 | 真实位图、生成图片或设计过的 SVG 资产 | emoji 占位 |

如果项目没有图标库，先提出选型建议，不要临时手写一堆图标。

## PlayGround 与交互验证

涉及 UI 组件、动效、复杂交互或视觉资产时，优先先做独立 PlayGround。

| 场景 | PlayGround 要求 |
|---|---|
| 新 UI 组件 | 提供独立 demo，先调组件再接业务 |
| 页面动效 | 提供静态或最小可运行演示 |
| AI 对话功能 | 提供简单对话测试页，并列出所有 prompt |
| 设计变更 | 同步更新 demo，只增改，不静默删除 |

所有支持回车发送的输入框，必须处理输入法组合状态：

```ts
function handleKeyDown(e: React.KeyboardEvent) {
  if (e.key === 'Enter' && !e.shiftKey && !e.nativeEvent.isComposing) {
    e.preventDefault()
    handleSend()
  }
}
```

禁止只判断 `e.key === 'Enter'`。中文、日文、韩文输入法确认候选词时不应触发发送。

## 安全与风险刹车

触碰以下内容前，必须停下来请求确认：

- 密钥、token、认证、权限、支付、账单、生产数据、用户隐私。
- 数据库删除、迁移回滚、不可逆数据变更、外部服务副作用。
- 影响安全、构建、运行时或许可证的依赖升级。
- 大规模重写、架构变化、全局格式化。
- 会扩大 AI 自主权、网络访问或工具权限的 prompt、agent、tool 变更。

默认最小权限。不要打印密钥。未经用户明确授权，不要把私密数据放进提示词。

## 依赖、配置与不可逆操作

修改依赖、配置、数据库、部署和环境变量时，必须主动说明影响。

| 类型 | 必须说明 |
|---|---|
| 依赖变更 | 新增、移除或升级了什么，为什么需要，版本选择依据 |
| 配置变更 | 影响哪些环境，默认值是什么，是否需要迁移 |
| 数据库结构或数据 | 备份位置、回退方案、预计恢复步骤 |
| 外部服务 | 会产生哪些真实调用、费用、限流或副作用 |
| 环境变量 | 变量名、用途、是否必填，不得写入真实密钥 |

涉及 `drop`、`delete`、`alter`、迁移回滚、批量覆盖、生产配置修改等不可逆操作时，
必须先给出备份和回退方案。没有回退方案，不执行。

## 调试与日志规则

修 Bug 前先定位完整链路，禁止猜测性修复。

| 步骤 | 要求 |
|---|---|
| 复现 | 说明触发条件、现象、期望结果 |
| 链路 | 梳理上游输入、核心处理、下游影响 |
| 证据 | 用日志、断点、测试脚本或最小复现验证假设 |
| 修复 | 只改根因，不顺手改旁支 |
| 回归 | 声明影响范围并运行相关检查 |

日志要求：

- 前端日志包含请求参数、响应摘要、错误堆栈。
- 后端日志包含请求来源、处理阶段、错误详情、时间戳。
- 不允许空 `catch`、`pass`、`ignore` 或只 `console.log(e)` 后继续吞错。
- 临时调试日志、脚本和开关在交付前必须清理；若需保留，说明原因。

每次代码变更后，必须声明可能影响的功能范围。

## 测试与真实接口

交付前必须选择与风险匹配的验证方式。

| 场景 | 最低验证 |
|---|---|
| 纯文档 | Markdown 结构检查或人工读检 |
| 工具函数 | 单元测试或等价脚本验证 |
| API | 请求/响应、状态码、错误路径 |
| UI | 浏览器运行、截图或关键交互验证 |
| 数据处理 | 边界值、空数据、异常数据 |
| AI 模型调用 | 真实 Key 到位后发起一次可用性测试 |

涉及真实 AI、支付、邮件、短信、Webhook、搜索、云服务等外部接口时，不得用硬编码假响应冒充完成。
如果缺少 API Key 或权限，必须明确说明缺少什么，并把任务标记为 Blocked。

## Multi-Agent 规则

默认使用一个强 coding agent。

只有明确增益时才使用子 agent：

| 场景 | 使用原因 |
|---|---|
| 源码调研 | 并行阅读互不依赖的模块 |
| 设计审查 | 获得独立 UX 或视觉判断 |
| 安全审查 | 获得新的风险视角 |
| 测试规划 | 扩展边界情况和异常路径 |

不要因为 multi-agent 听起来高级就引入。必须能证明它提升了质量、速度或安全性。

## 完成定义

只有同时满足以下条件，任务才算完成：

- PRD 或 Todo 目标已满足。
- diff 严格服务于用户请求。
- 相关检查已通过，或失败被明确披露。
- 没有未经批准的删除。
- 没有偷偷实现未写入 PRD 的需求变化。
- 剩余风险和后续事项已说明。

## 版本记录与发布检查

项目如果存在 `docs/FEATURES.md`、`docs/CHANGELOG.md`、`docs/RELEASE_NOTES.md`
或等价文档，AI 必须按项目约定更新。

| 文档 | 记录内容 | 触发时机 |
|---|---|---|
| `docs/FEATURES.md` | 功能生命周期、状态、方案变化和取消原因 | 新想法、开始开发、完成、取消 |
| `docs/CHANGELOG.md` | 技术改动、根因、影响面、验证状态 | Bug 修复、功能完成、重构、性能优化 |
| `docs/RELEASE_NOTES.md` | 用户能感知的新功能、改进、修复 | 准备发布给真实用户 |
| `docs/METHODOLOGY.md` | 可复用的产品原则、设计偏好、反模式 | 用户表达稳定偏好或取舍逻辑 |

记录规则：

- 时间必须读取当前系统时间，不凭记忆填写。
- 取消的功能也要保留记录，标明取消原因。
- Release Notes 只写用户感知变化，不写调试细节和文件路径。
- 方法论只记录可复用原则，不记录一次性临时决定。

发布前必须做 diff 安全检查：

| 检查项 | 要求 |
|---|---|
| Release Notes vs diff | 实际改动必须能对应到发布说明 |
| 未提及文件 | 发现未说明的配置、依赖、核心逻辑改动时暂停 |
| 删除或替换 | 发现已有逻辑被删除或替换时暂停 |
| 环境和权限 | 发现环境变量、权限、部署配置变化时暂停 |

存在偏差时，先输出风险报告并等待用户确认，不继续发布。

## 用户纠偏信号

当用户发出这些信号，AI 必须立刻校准状态：

| 用户信号 | 含义 | AI 应对 |
|---|---|---|
| “现在做到哪了？” | 状态不透明 | 汇报阶段、目标、下一步 |
| “为什么改这个？” | 范围可能漂移 | 把 diff 映射回 Todo |
| “这不是我的意思” | PRD 不匹配 | 停止实现，更新 PRD，再继续 |
| “这个安全吗？” | 风险解释不足 | 给出风险和审批表 |
| “验证了吗？” | 完成声明不可靠 | 运行检查，或说明不能验证的原因 |

## Session 生命周期管理

对话超过 10 轮后，执行关键操作前必须复述当前目标和关键约束。
单 session 超过 30 轮对话，或功能方向发生重大变化时，必须开新 session。
旧 session 的关键信息必须带到新 session。

| 必带信息 | 说明 |
|---|---|
| PRD 当前版本 | 产品定义和已变更的范围 |
| 已完成的 Todo | 做了什么，当前状态 |
| 关键技术决策 | 选型、约束、已知的坑 |
| 未解决的问题 | 待办、风险、隐患 |
| 品牌和风格约束 | 调性、文案风格、设计规范 |

恢复锚点：用不超过 300 字总结"在做什么、最后有意义的进展、下一步计划"，
新 session 开头读取恢复锚点，对齐后再动手。

如果多个 agent、多个终端或多次编辑可能触碰同一文件，后续修改前必须读取文件最新版本。
禁止基于记忆中的旧内容继续编辑。

## 用户反馈规则

### 反馈精度

给 AI 的反馈必须包含三要素：区域 + 现象 + 期望效果。

| 类型 | 错误示范 | 正确示范 |
|---|---|---|
| 布局问题 | 排版太丑了，乱七八糟 | 项目列表区的标签文字上下不居中，容器框右侧截断了文字，期望标签垂直居中且容器自适应宽度 |
| 功能问题 | 这个功能不行 | 点击标签筛选后，列表没有刷新到第一页，期望切换标签后自动回到第一页 |
| 视觉问题 | 太差劲了 | 筛选区的高亮把文字设成了白色，浅色背景上看不清，期望高亮时保持文字可读 |

禁止使用纯评价性语言。AI 无法从情绪词中提取可执行信息。

### 消息负载上限

一条消息最多传达一个功能模块的需求。

| 场景 | 做法 |
|---|---|
| 单个小修改 | 直接说 |
| 一个完整功能 | 一条消息说目标 + 3 至 5 个子需求 |
| 多个功能模块 | 先更新 Todo，逐条下发，不塞在一条消息里 |

违反负载上限的信号：消息超过 200 字、出现"另外""同时""还有"连接多个不相关需求。

### 视觉问题标注优先

涉及 UI 布局、样式、交互问题时，优先使用截图标注工具传递信息。

| 方式 | 适用场景 | 优先级 |
|---|---|---|
| 截图标注 | 布局错位、视觉异常、交互问题 | 最高 |
| 参考图 + 文字说明 | 设计方向调整、风格参考 | 高 |
| 纯文字描述 | 逻辑问题、数据问题、文案问题 | 正常 |

不要用纯文字描述视觉效果。截图标注比"看起来太挤了"有效十倍。

## 中文文案与 Prompt 自查

当 AI 生成产品文案、说明文字、对话稿、System Prompt、User Prompt 或 Few-shot
示例时，交付前必须做一次轻量自查。

| 检查项 | 要求 |
|---|---|
| 句式 | 少用「不是 A，而是 B」式对比，优先正向陈述 |
| 标点 | 避免滥用破折号、省略号和英文引号 |
| 语气 | 不使用空泛夸赞、煽情词和网文式情绪表达 |
| 信息 | 直接给判断、规则或动作，不铺垫过多解释 |
| Prompt | 系统提示词同样遵守清晰、可执行、少情绪的原则 |

如果输出是正式文案，最后必须说明已完成文案自查。

## 功能膨胀控制阀

新功能加入前，必须回答三个问题：

1. 它服务哪个核心用户场景？
2. 不做会怎样？
3. 它是否需要新的数据结构、UI 区域或外部依赖？

前两条答不上来，不加。

如果功能已经偏离 PRD 的原始范围，先更新 PRD 的"范围"和"非范围"，再决定是否实现。

## 先构思后写码

复杂功能必须走两轮：

| 轮次 | 做什么 | 产物 |
|---|---|---|
| 第一轮 | 讨论方案，不写代码 | 方案文档或 PRD 更新 |
| 第二轮 | 确认方案后实现 | 可验证的代码 |

简单修改和 bug 修复不需要走两轮。

判断信号：需求涉及多文件联动、新数据结构、新 UI 区域、外部 API 调用时，
强制走第一轮。

## Git 版本安全网

每个 Todo 完成并验证后，必须 commit。

| 时机 | 操作 |
|---|---|
| Todo 完成并验证 | commit，message 说明做了什么 |
| 大改前（全局重写、架构调整） | 创建新分支 |
| AI 改坏了 | git revert 回到上一个 commit |
| 功能方向不确定 | 在分支上实验，确认后再 merge |

不要让 AI 连续修改 5 次以上还不 commit。一旦改坏，无法回退。

## 极简提醒

PRD 定方向，Todo 定节奏，源码定真相，测试定完成。
不清楚就问，高风险就停，改了就验证，跑偏就回 PRD。
一条消息一件事，反馈说事实不说情绪。
改完就 commit，迷路就开新 session。
